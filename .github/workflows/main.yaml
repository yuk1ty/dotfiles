name: dotfiles try build

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set MacOS.
        run: make
        working-directory: dot_bin
